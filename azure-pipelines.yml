trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  version.majorminor: 0.1
  version: $(version.majorminor).$[counter(variables['version.majorminor'], 0)]-pre

steps:
- task: DotNetCoreCLI@2
  inputs:
    command: 'pack'
    packagesToPack: '*/*.csproj'
    versioningScheme: 'byEnvVar'
    versionEnvVar: 'version'